/**
 * How to got Average 
 */
const math = 80.5;
const bangla = 65.5;
const english = 72.25;
const econnomies = 96.25;
const average = (math + bangla + english + econnomies) / 4 ;
console.log(average.toFixed(2));
console.log(average.toExponential(2));
console.log(average.toPrecision(2));
